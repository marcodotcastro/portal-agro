# Generated by VDD Builder from the workspace manifest.
# Workspace portal-agro

Estas regras descrevem somente o workspace gerado e seus contratos observados.

## Limites autorizados

- A raiz operacional é `/home/marcodotcastro/Work/orca-workspaces/powerful-brain/vdd/portal-agro-workspace`.
- Repositórios de produto ficam isolados em subdiretórios de `repos`, um por papel, e não recebem arquivos de controle do workspace.
- Arquivos gerados pelo Builder ficam em `docker/`, `docker-compose.yml`, `AGENTS.md` e `CONTEXT.md`.
- Não armazenar credenciais, tokens ou segredos neste workspace.
- Não criar commits automaticamente; mudanças de produto permanecem nos repositórios correspondentes.

## Comandos autorizados

```text
vdd builder init "portal-agro" --json
vdd builder setup "portal-agro" --yes --json
vdd builder up "portal-agro" --json
vdd builder status "portal-agro" --json
vdd builder down "portal-agro" --json
vdd builder test "portal-agro" --json
docker compose config --quiet
```

## Contrato do manifesto

- `schema`: `vdd/workspace-manifest/v1`.
- `name`: `portal-agro`; `slug`: `portal-agro`.
- `status`: `configured`.
- Componentes registrados: `app`.
- Relações registradas: nenhuma.
- Runtime declarado: `docker_compose`; especificação: `.workspace/projects/portal-agro/specs/index.yaml`.

## Componentes

| Papel | Origem | Ref | Destino | Linguagem/stack | Frameworks | Versões | Gerenciador | Dependências | Manifestos | Lockfiles | Scripts | Portas |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `app` | `git@github.com:marcodotcastro/portal-agro.git` | `main` | `repos/app` | `ruby`, `node` / rails | `rails` | `node=>=22 <23`, `rails=5.2.2.1`, `ruby=2.6.7` | yarn | `postgres` | `composer_json=false`, `composer_lock=false`, `gemfile=true`, `gemfile_lock=true`, `go_mod=false`, `go_sum=false`, `npm_shrinkwrap=false`, `package_json=true`, `package_lock=false`, `pipfile_lock=false`, `pnpm_lock=false`, `poetry_lock=false`, `requirements_lock=false`, `requirements_txt=false`, `requirements_txt_lock=false`, `yarn_lock=true` | `Gemfile.lock`, `yarn.lock` | `bmad:install=npm exec --yes --package=node@22 --package=bmad-method -- bmad-method install --directory . --modules bmm --tools none --yes`, `bmad:update=npm exec --yes --package=node@22 --package=bmad-method -- bmad-method install --directory . --action update --yes` | `3003`, `3000` |

## Runtime calibrado

- `repos/app` fixa Ruby `2.6.7`, Rails `5.2.2.1`, Bundler `1.17.3`, Node `22` e Yarn `1.22.22`.
- O app recebe `DB_HOST=postgres`, `DB_PORT=5432`, `DB_USERNAME=postgres`, `DB_PASSWORD=postgres` e usa `portal_agro_development`.
- O comando do serviço prepara o banco com `rails db:create` e `rails db:migrate` antes de iniciar o Puma na porta `3003`.
- O Action Cable de desenvolvimento usa `async`; não há serviço Redis no runtime Docker.

## Mapa de serviços

| Serviço | Imagem/Dockerfile | Portas | Dependências | Rede | Health-check |
| --- | --- | --- | --- | --- | --- |
| `app` | `docker/Dockerfile.app` | `3003:3003` | `postgres` | `workspace` | declarado |
| `postgres` | `postgres:16-alpine` | `não declarado` | nenhuma | `workspace` | declarado |

## Relações

Nenhuma relação foi declarada.
