#!/bin/bash
set -e

# If running the rails server then create or migrate existing database
if [ "${*@M}" = "./bin/rails server" ] || [ "${*@M}" = "bundle exec rails server" ] || [ "${*@M}" = "bin/rails server" ]; then
  ./bin/rails db:prepare
fi

exec "$@"
